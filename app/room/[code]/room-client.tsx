"use client"

export function RoomClient({ code }: { code: string }) {
  // This component handles client-side effects like confetti
  // We could use a polling mechanism here to refresh data if we wanted real-time updates without reload

  return null
}
